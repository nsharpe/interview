# Media Player Endpoint

Media metrics capture for playing time based media.

### Accessing the application via `run_locally.sh`

To see a list of all the endpoints, while the application is running go to
[swagger](http://localhost:9100/swagger-ui/index.html)

The health of the system can be viewed through
[healthcheck](http://localhost:9101/actuator/health)
