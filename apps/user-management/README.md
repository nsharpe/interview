# User Management

Service for managing user accounts, authentication, and user data.

### Accessing the application via `run_locally.sh`

To see a list of all the endpoints, while the application is running go to
[swagger](http://localhost:9080/swagger-ui/index.html)

The health of the system can be viewed through
[healthcheck](http://localhost:9081/actuator/health)
