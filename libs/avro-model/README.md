# Avro Model

Avro schema definitions and generated Java classes for Kafka message serialization.

Uses the Gradle Avro plugin to generate Java classes from `.avsc` schemas in `src/main/avro`. Depends on `kafka-avro-serializer` and `jackson-dataformat-avro`.
