# Spring Pod

## Purpose
When using a pod in kubernetes etc, this module should be included.  This will set base metrics health checks etc

### Interview Specific Comments
The actual implementation is not included in this as in a real world scenario there would be a task to perform the implementation in parallel with other work.