# Security Flux

WebFlux variant of the [security module](../security/README.md). Provides Spring Security configuration and token-based authentication for reactive applications.

Depends on [redis-driver](../../drivers/redis-driver/README.md) for token storage and uses `reactor-test` for testing.
